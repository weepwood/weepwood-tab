# 桌面编辑与历史记录

- 进入编辑模式后，可使用底部编辑工具栏撤销或重做操作。
- 键盘快捷键：`Ctrl/Cmd + Z` 撤销，`Ctrl/Cmd + Shift + Z` 重做。
- 历史记录最多保留 50 个状态快照，不写入持久化文件。
- 在项目右键菜单中可锁定位置。锁定后禁止移动、缩放和拖拽合并。
- 编辑模式下拖动快捷方式图标到 Dock，可直接固定到 Dock。
- 输入框、文本域和可编辑内容聚焦时，不拦截浏览器原生撤销快捷键。
